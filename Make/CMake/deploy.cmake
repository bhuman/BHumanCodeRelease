if(WINDOWS)
  add_custom_target(deployNao "cmd.exe" /c "bash -c \"if [ -f ../../Build/Linux/Nao/$<CONFIG>/success ]; then ./deployDialog $<CONFIG>; fi\"" WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/../Common")
  set_property(TARGET deployNao PROPERTY FOLDER Utils)
  add_dependencies(deployNao Nao)
  add_dependencies(deployNao DeployDialog)

  add_custom_target(deployBooster "cmd.exe" /c "bash -c \"if [ -f ../../Build/Linux/Booster/$<CONFIG>/success ]; then ./deployDialog $<CONFIG>; fi\"" WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/../Common")
  set_property(TARGET deployBooster PROPERTY FOLDER Utils)
  add_dependencies(deployBooster Booster)
  add_dependencies(deployBooster DeployDialog)
endif()
