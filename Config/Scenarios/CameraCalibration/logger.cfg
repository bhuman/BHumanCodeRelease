// Is logging enabled?
enabled = false;

// The directory that will contain the log file.
path = "/home/nao/logging";

// The number of buffers allocated.
numOfBuffers = 6000;

// The size of each buffer in bytes.
sizeOfBuffer = 800000;

// The scheduling priority of the writer thread.
writePriority = -2;

// Logging will stop if less MB are available to the target device.
minFreeDriveSpace = 100;

// Representation that can be logged from every thread
loggablePerThread = [
  MotionRequest,
];

// Representations to log per thread
representationsPerThread = [
  {
    thread = Upper;
    representations = [
      JPEGImage,
      BodyContour,
      CameraInfo,
      CameraMatrix,
      FieldBoundary,
      FrameInfo,
      ImageCoordinateSystem,
      LinesPercept,
      OdometryData,
    ];
  },
  {
    thread = Lower;
    representations = [
      JPEGImage,
      BodyContour,
      CameraInfo,
      CameraMatrix,
      FieldBoundary,
      FrameInfo,
      ImageCoordinateSystem,
      LinesPercept,
      OdometryData,
    ];
  },
  {
    thread = Cognition;
    representations = [
      ActivationGraph,
      AgentStates,
      AlternativeRobotPoseHypothesis,
      ArmMotionRequest,
      BehaviorStatus,
      CalibrationRequest,
      CameraCalibration,
      CameraCalibrationStatus,
      GameControllerData,
      GameState,
      HeadMotionRequest,
      IMUCalibration,
      IndirectKick,
      MotionRequest,
      Odometer,
      OdometryData,
      ReceivedTeamMessages,
      RobotHealth,
      RobotPose,
      SelfLocalizationHypotheses,
      SideInformation,
      SkillRequest,
      StrategyStatus,
      TeamBallModel,
      TeamData,
    ];
  },
  {
    thread = Motion;
    representations = [
      FallDownState,
      FootSupport,
      FrameInfo,
      FsrSensorData,
      GroundContactState,
      HeadMotionInfo,
      IMUValueState,
      InertialSensorData,
      InertialData,
      JointAnglePred,
      JointCalibration,
      JointPlay,
      JointRequest,
      JointSensorData,
      KeyStates,
      MotionInfo,
      MotionRequest,
      MotionRobotHealth,
      OdometryData,
      OdometryDataPreview,
      RawInertialSensorData,
      RobotStableState,
      SolePressureState,
      SystemSensorData,
      WalkLearner,
      WalkStepData,
    ];
  }
];
