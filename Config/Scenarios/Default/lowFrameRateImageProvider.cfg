// Framerates per minute
frameRate = 120;
conditionFrameRate = 240;

// If true, only images are logged where any of the given conditions (below) is matched.
onlyLogConditions = true;

// Conditions are pre-defined in LowFrameRateImageProvider::generateConditions().
// For the most percepts conditions are already predefined, e.g. BallPercept.
// Invalid conditions are reported (not asserted!) at runtime.
conditions = [
    BallPercept
];
