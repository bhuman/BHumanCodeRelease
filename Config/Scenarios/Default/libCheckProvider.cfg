// Configuration of reaction (ignore, warn, abort) to failed checks for different outputs.
notSetReaction = {
  motionRequest = warn;
  headMotionRequest = warn;
  calibrationFinished = ignore;
  passTarget = ignore;
};
multipleSetReaction = {
  motionRequest = warn;
  headMotionRequest = warn;
  calibrationFinished = warn;
  passTarget = warn;
};
