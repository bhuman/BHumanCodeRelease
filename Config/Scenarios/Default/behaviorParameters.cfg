keeperJumpingOn = true; // jumping keeper is activated
penaltyStrikerWalkSpeed = 0.5; // penalty striker must not walk at full speed; penalty keeper also uses this value
penaltyStrikerAngleToLeftPostOffset = 10deg;
penaltyStrikerAngleToRightPostOffset = 10deg;
penaltyStrikerUseObstacles = true;
ballCatchMaxWalkDistance = 1200;
standRadius = 60;
walkRadius = 125;
genuflectRadius = 200;
genuflectStandRadius = 200;
jumpRadius = 600;
