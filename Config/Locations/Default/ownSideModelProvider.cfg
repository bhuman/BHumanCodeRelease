// Parameter file for the OwnSideModelProvider

distanceUncertaintyOffset = 300; // Estimated odometry and localization base error (mm).
distanceUncertaintyFactor = 1.3; // Estimated odometry error as a factor of the distance walked.
largestXInInitial = -700; // The largest x coordinate of a robot in initial (mm).
awayFromLineDistance = 80; // The distance the robot has to before or behind a certain line (mm).
minPenaltyTime = 23000; // The minimum time a robot must be penalized to actually believe it. (ms).
minPenaltyTimeIP = 8000; // The minimum time a robot must be penalized for Illegal Positioning to actually believe it. (ms).
