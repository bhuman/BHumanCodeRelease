retries = 10;           //**< Number of tries to open device. */
retryDelay = 500;       //**< Delay before a retry to open device. */
channels = 2;           //**< Number of channels to capture. */
sampleRate = 8000;      //**< Sample rate to capture. This variable will contain the framerate the driver finally selected. */
maxFrames = 5000;       //**< Maximum number of frames read in one cycle. */
onlySoundInSet = false; //**< If true, the module will not provide audio data in game states other than set */