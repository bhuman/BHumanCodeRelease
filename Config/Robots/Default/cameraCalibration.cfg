cameraRotationCorrections = {
  lower = {
    x = 0deg;
    y = 0deg;
    z = 0deg;
  };
  upper = {
    x = 0deg;
    y = 0deg;
    z = 0deg;
  };
};
bodyRotationCorrection = {
  x = 0deg;
  y = 0deg;
};
